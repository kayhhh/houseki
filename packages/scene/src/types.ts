export enum MaterialAlphaMode {
  OPAQUE = 0,
  MASK = 1,
  BLEND = 2,
}

export enum KeyframePath {
  POSITION = 0,
  ROTATION = 1,
  SCALE = 2,
  WEIGHTS = 3,
}

export enum KeyframeInterpolation {
  LINEAR = 0,
  STEP = 1,
  CUBICSPLINE = 2,
}
