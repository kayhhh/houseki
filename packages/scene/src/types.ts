export enum MaterialAlphaMode {
  OPAQUE = 0,
  MASK = 1,
  BLEND = 2,
}

export enum ImageMimeType {
  "image/jpeg" = 0,
  "image/png" = 1,
  "image/webp" = 2,
  "image/gif" = 3,
  "image/bmp" = 4,
}
